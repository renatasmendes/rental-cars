<h1>Fabricantes</h1>

<% @manufacturers.each do |m| %> <!-- @manufacturers vem da controller -->
    <h3><%= link_to m.name, manufacturer_path(m.id) %></h3> <!-- manufacturer no singular pq no routes, o prefixo é esse + "_path" pra visualizar um unico registro na tela -->
<% end %>

<% if @manufacturers.empty? %>
  <p>Nenhum fabricante cadastrado</p>
<% end %>

<%= link_to 'Registrar novo fabricante', new_manufacturer_path %>

<%= link_to 'Voltar', root_path %>
