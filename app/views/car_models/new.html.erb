<h1>Cadastrar modelo de carro</h1>

<%= form_with(model: CarModel.new, local: true) do |f| %>
  <div>
    <%= f.label :name, 'Nome' %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.label :fuel_type, 'Combustível' %>
    <%= f.text_field :fuel_type %>
  </div>
  <div>
    <%= f.label :motorization, 'Motor' %>
    <%= f.text_field :motorization %>
  </div>
  <div>
    <%= f.label :year, 'Ano' %>
    <%= f.text_field :year %>
  </div>
  <div>
    <%= f.label :manufacturer_id, 'Fabricante' %> <!-- aqui vai manufacturer_id pq é o valor armazenado em car_model-->
    <%= f.collection_select(:manufacturer_id, Manufacturer.all, :id, :name) %> <!-- o primeiro é a coluna manufacturer_id que é a coluna que está em CarModel pois lá não é armazenado o nome e sim o id-->
                                                                               <!-- o segundo é Manufacturer.all que são todos os fabricantes cadastrados no banco. -->
                                                                               <!-- O terceiro, é o id dele no controller do manufacturer-->
                                                                               <!-- o quarto elemento, é o que vai exibir na tela pro usuário-->
  <div>
    <%= f.label :car_category_id, 'Categoria' %> 
    <%= f.collection_select(:car_category_id, CarCategory.all, :id, :name) %> 
  </div>
  <%= f.submit 'Enviar' %> 
<% end %>