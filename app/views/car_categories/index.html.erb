<h1>Categorias de Carros</h1>
<% if @car_categories.any? %>
<table>
  <thead> 
    <tr> 
      <th>Categoria</th> 
      <th>Diária</th>
    </tr>
  </thead>
  <tbody> 
  <% @car_categories.each do |category| %>
    <tr>
      <td><%= link_to "Categoria #{category.name}", category %></td>
      <td><%= number_to_currency category.daily_rate %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<% else %>
  <p>Nenhuma categoria cadastrada</p>
<% end %>
<%= link_to 'Registrar nova categoria de carro', new_car_category_path %>
<%= link_to 'Voltar', root_path %>